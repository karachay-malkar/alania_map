name: Build Alan Map 10.0 mountain base — disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy 10.0 generator is disabled for the 12.1 architecture."
